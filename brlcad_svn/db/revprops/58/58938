K 10
svn:author
V 9
tbrowder2
K 8
svn:date
V 27
2013-12-16T12:17:37.549705Z
K 7
svn:log
V 491
updates to get BRL-CAD C source files compiled warning- and error-free
  with a C++ compiler:

changing all bu_rb_free function signatures to be
the same with explict function pointer arguments:

  void bu_rb_free(struct bu_rb_tree *tree, void (*free_data)(void *data));

also removed unneeded code from the actual call of the function pointer

successful debug build on Debian 7, 64-bit:
  no warnings or errors (except for the usual from libgiam, tcl, and step)

successful 'make regress'

END
